@model List<CarPurchasing_DAL.Models.Dto.Res.ResTypeDto>
@{
  Layout = "_LayoutAdmin";
  ViewData["Title"] = "Master Type";
}

<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <div class="content-header">
      <h1>Halaman Master Type</h1>
    </div>
    <div class="content text-right">
      <a asp-controller="Type" asp-action="FormTambahType" class="btn btn-primary">Tambah Data</a>
    </div>
    <br>
    <!-- /.content-header -->

    <!-- Main content -->
    <section class="content">
      <table class="table">
        <thead>
          <tr>
            <th scope="col">Name</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          @foreach (var dataType in Model)
          {
            <tr>
              <td>@dataType.Name</td>
              @*
                <td>
                    <a data-id="@dataBrand.Id" data-username="@dataBrand.Carname" href="#" class="btn btn-danger deleteCar">Delete</a>
                    <a data-id="@dataBrand.Id" data-username="@dataBrand.Carname" href="#" class="btn btn-primary editCar">Edit</a>
                </td>
              *@
            </tr>
          }
        </tbody>
      </table>
    </section>
    <!-- /.content -->
</div>
